# chuahpstix-league-viz

Simple static web app that visualizes **chuahpstix** as a League player profile with favorite stats.

## Local preview
Open `index.html` directly, or run a simple static server.

## Deploy
Optimized for Vercel auto-deploy on push.
